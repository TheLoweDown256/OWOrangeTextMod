# Mod That Automatically Colours All Jam Themes Orange

Adds the <color=orange><\/color> tag to all instances of "clockwork", "layers", "bigger on the inside", "hidden in plain sight", "community", "escape", "isolation", "miniature", "past", and "future". Also applies to different tenses like "escaping" and "layered".
